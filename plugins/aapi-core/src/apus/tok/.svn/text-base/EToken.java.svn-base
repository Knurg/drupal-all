package apus.tok;

import lebada.fs.CFS;

public class EToken extends Token {

	public CFS info;

	public EToken(String surface, int type, CFS info) {
		super(surface, type);
		this.info = info;
	}
	
	public EToken(String surface, int type) {
		super(surface, type);
		this.info = null;
	}
	
	
}
